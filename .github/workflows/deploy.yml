name: Deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app" \
            -H "Content-Type: application/json" \
            -d '{"app": "testappnov5-1762321004990", "repo": "https://github.com/BharatSharma34/testappnov5-1762321004990.git", "github_token": "${{ secrets.PRIVATE_GITHUB_TOKEN }}"}'